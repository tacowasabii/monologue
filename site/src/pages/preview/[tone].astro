---
// 색 톤 미리보기. 고르고 나면 이 폴더째 지운다.
import Landing from '../../layouts/Landing.astro';

export function getStaticPaths() {
  return ['a', 'b', 'c', 'd'].map((tone) => ({ params: { tone } }));
}

const { tone } = Astro.params;
---

<Landing tone={tone} noindex />
