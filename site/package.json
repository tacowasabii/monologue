{
  "name": "monologue-site",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "astro build && vitest run"
  },
  "dependencies": {
    "@astrojs/vercel": "^11.0.10",
    "@upstash/redis": "^1.38.4",
    "astro": "^7.3.2"
  },
  "devDependencies": {
    "vitest": "^5.0.0"
  }
}
